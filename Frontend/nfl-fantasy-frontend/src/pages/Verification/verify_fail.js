// Page for when verification for a new user registration fails

const VerifyFail = () => {
    return (
        <div>
            <h2 style={{ paddingLeft: '20px' }}>Verification failed, please try again.</h2>
        </div>
    );
};

export default VerifyFail; 
