// Page for when verification for a new user registration is successful

const VerifySuccess = () => {
    return (
        <div>
            <h2 style={{ paddingLeft: '20px' }}>Verification successful, you may now login.</h2>
        </div>
    );
};

export default VerifySuccess; 